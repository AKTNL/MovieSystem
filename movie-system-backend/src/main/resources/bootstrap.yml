server:
  port: 8080

spring:
  application:
    name: movie-system # 服务名称，非常重要！Nacos用这个名字找配置
  cloud:
    nacos:
      server-addr: localhost:8848 # Nacos 地址
      config:
        file-extension: yaml # 配置文件格式
        group: DEFAULT_GROUP
        # 自动刷新配置
        refresh-enabled: true